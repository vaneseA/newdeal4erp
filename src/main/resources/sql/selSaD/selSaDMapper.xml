<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.happyjob.study.selSaD.model.SelSaDModel">

    <!--  목록 조회 -->
    <select id="saleMonthList" resultType="kr.happyjob.study.selSaD.model.SelSaDModel">
        SELECT
        od.order_date,
        od.order_no,
        cl.clnt_name,
        pc.pro_name,
        sl.splr_name,
        pd.product_name,
        pd.product_serial,
        pd.product_price,
        pd.product_unit_price,
        odt.order_dt_amt,
        od.order_tot_price
        FROM
        tb_order od
        INNER JOIN tb_clnt cl ON od.clnt_no = cl.clnt_no
        INNER JOIN tb_order_dt odt ON od.order_no = odt.order_no
        INNER JOIN tb_product pd ON odt.product_no = pd.product_no
        INNER JOIN tb_pro pc ON pd.pro_cd = pc.pro_cd
        INNER JOIN tb_splr sl ON pd.splr_no = sl.splr_no

        <where>
            <if test="(proName != null) and (!proName.equals(''))">
                and pc.pro_name = #{proName}
            </if>
            <if test="(splrName != null) and (!splrName.equals(''))">
                and sl.splr_name = #{splrName}
            </if>
            <if test="(pname != null) and (!pname.equals(''))">
                <choose>
                    <when
                            test="searchKey eq 'product_name'.toString()">
                        and pd.product_name Like CONCAT('%', #{pname}, '%')
                    </when>
                    <otherwise>
                        and (   pd.product_name Like CONCAT('%', #{pname}, '%')
                        )
                    </otherwise>
                </choose>
            </if>
        </where>
        ORDER BY od.order_date
        LIMIT #{pageindex}, #{pageSize}

    </select>

</mapper>